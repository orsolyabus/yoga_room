  <h2>Teachers</h2>

  <div class="row ">
    <% @users.each do |user| %>
      <div class="col-md-6 col-lg-4" >
         <div class="card shadow-sm my-2"  > 
          <div class="card-body">
            <h5 class="card-title"><%= user.name%> - <%= user.info&.spiritual_name %></h5>
            <p><%= user.yoga_classes.count %> classes</p>
            <%= link_to 'Show', user_path(user), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
